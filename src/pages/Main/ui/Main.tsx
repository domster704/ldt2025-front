import React, {FC} from 'react';
import {useAppDispatch} from "@app/store/store";


const Main: FC = () => {
  const dispatch = useAppDispatch();

  return (
    <div>
      1
    </div>
  );
}

export default Main;