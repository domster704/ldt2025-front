/**
 * Список доступных цветов для **предупреждающих состояний** КТГ.
 *
 * Используется для настройки цветовой индикации ухудшения или негативного прогноза.
 *
 * По умолчанию включает несколько вариантов:
 * - `#e87000` — оранжевый,
 * - `#e82700` — красный,
 * - `#3a00e8` — фиолетовый,
 * - `#e80080` — розовый.
 *
 * Пользователь может выбрать один из этих цветов через {@link ColorsPicker},
 * и выбранное значение сохраняется в Redux + localStorage.
 */
export const availableWarningColors: string[] = [
  "#e87000",
  "#e82700",
  "#3a00e8",
  "#e80080",
];

/**
 * Список доступных цветов для **нормального состояния** КТГ.
 *
 * Используется для отображения состояния пациента в пределах нормы.
 *
 * По умолчанию доступны:
 * - `#00a619` — зелёный,
 * - `#0097df` — голубой.
 *
 * Пользователь выбирает цвет через {@link ColorsPicker},
 * и он сохраняется в Redux + localStorage.
 */
export const availableGoodColors: string[] = [
  "#00a619",
  "#0097df"
];
